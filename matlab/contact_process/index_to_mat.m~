% Helpers
function mat = index_to_mat(i,len)
    mat = zeros([1 3*len]);
    str = dec2bin(i-1);
    j = 1;
    for i = (3*len-length(str)+1):3*len
        mat(i) = str2double(str(j)); j = j + 1;
    end
    resp
end